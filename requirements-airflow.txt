# Optional - Phase 3 orchestration layer
# Install in a SEPERATE virtual environment / container from requirments.txt:
#
#   python -m venv .venv-airlflow && soruce .venv-airlow/bin/activate
#   AIRFLOW_VERSION=2.9.3
#   PYTHON_VERSION="$(python -c 'import sys; print(f"{sys.version_info.major}.{sys.version_info.minor}")')"
#   CONSTRAINT_URL="https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-${PYTHON_VERSION}.txt"
#   pip install "apache-airflow==${AIRFLOW_VERSION}" --constraint "${CONSTRAINT_URL}"
#
# See: https://airflow.apache.org/docs/apache-airflow/stable/installation/installing-from-pypi.html
apache-airflow==2.9.3