# --- Core data stack -------------------------------------------------------
pandas>=2.0
numpy>=1.24

# --- Database ----------------------------------------------------------------
sqlalchemy>=2.0
psycopg2-binary>=2.9          # PostgreSQL driver (only needed for the Postgres backend)

# --- Statistics / econometrics / ML -----------------------------------------
scikit-learn>=1.3
statsmodels>=0.14             # optional: SARIMA/ExponentialSmoothing extensions
xgboost>=2.0                  # optional: drop-in swap for GradientBoostingClassifier

# --- Visualization -------------------------------------------------------------
matplotlib>=3.7
plotly>=5.18

# --- API / data product -------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Testing / quality -------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
black>=24.0
ruff>=0.4

# NOTE: Apache Airflow (Phase 3 orchestration) is intentionally NOT listed
# here. Airflow pins very specific transitive dependency versions and will
# often conflict with the packages above if installed into the same
# environment. Install it in its own virtualenv/container using the
# official "constraints file" workflow -- see requirements-airflow.txt and
# the "Roadmap" section of README.md.